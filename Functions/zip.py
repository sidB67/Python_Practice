my_list = [1,2,3]
your_list = [10,20,30]

print(list(zip(my_list,your_list)))
print(my_list)

#this makes a tuple of the corresponding items of the iterables given to it
#example we have list of usernames and pass we can use this to group user and pass at same index